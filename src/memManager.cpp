#include <memManager.h>

template <typename T>
void MemoryManager::enqueue(T item){
	
}

template <typename T>
T MemoryManager::dequeue(){
	
}

bool MemoryManager::empty(){
	return head == tail;
}

bool MemoryManager::full(){
	
}